node_modules
dist
.wrangler
.dev.vars
*.local
.DS_Store
coverage
*.tsbuildinfo
.vite

# Large local artifacts (do not publish)
*.tar.gz

# Local Cloudflare API token for scripts (never commit)
.env.cf
